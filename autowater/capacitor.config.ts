import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.oliveman.autowater',
  appName: 'Autowater',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
